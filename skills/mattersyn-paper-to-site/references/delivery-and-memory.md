# MatterSyn publication and memory

Both `cuiyist/mattersyn` and `cuiyist/mattersyn-site` are public at the user's latest direction. The website is https://cuiyist.github.io/mattersyn-site/. The project repository contains code, reviewed data, project memory, skills, safe audit summaries and cleaned history. Complete papers/SI, raw caches, unreviewed candidates, detailed private audits and original history bundles remain local, outside both repositories. The review-cited old raw-source URL returned HTTP 200 anonymously after the project repository became public on 2026-09-24; current-branch cleanup does not remove every historical object or third-party cache. The original Sites address and old direct-copy helpers are historical. Follow the current README and release configuration; do not infer the checkout from an old absolute path in memory.

## Public project repository and public website

Keep complete PDFs/SI, extracted/OCR text, snippet caches, source page renders, private working crops, source-path maps and unredacted evidence archives local. Preserve those originals and their hashes for research and audit. The public source repository contains code, structured work, project memory, reusable skills and cleaned history; the separate public site contains reviewed reader data, citations, necessary runtime and release controls, concise reader-facing provenance, and selected original source figures. Pair each displayed figure with its citation, page/figure locator, supported record/sample assignment and asset provenance. This display choice does not establish verified publisher permission; keep rights status factual. An audit summary is not the original scientific audit artifact.

Build an explicit, separately staged public projection using the reviewed content allowlist and exact asset provenance registry. Keep unselected or unclassified source material private. For the original source figures selected for the MatterSyn reader, follow the user's explicit display preference while preserving any unresolved permission status; do not mark them rights-cleared solely because they are displayed. A PDF extension blacklist, directory name, or absence of credentials is insufficient. Avoid broad recursive copies into a public Git checkout; source and destination must be distinct. Use document IDs/collection names in public provenance instead of personal computer paths. Preserve private ID-to-path mappings.

Image provenance is per exact asset and source: retain the DOI, original source hash and locator, display hash, transformation, permission evidence/status, attribution and reviewer where available. A citation or open download access alone does not establish publisher permission. Under the current MatterSyn display preference, selected original source figures remain available in the reader even when separate permission evidence has not been verified; keep that status unresolved and do not describe the choice as legal clearance. For figures outside the selected set, retain summarized findings, sample boundaries and locators with a source link or a clearly labeled authored illustration. Do not fabricate or redraw an unobserved measurement. Computational molecular/crystal models and apparatus/morphology illustrations keep their source and interpretation qualifications.

## One source and one release

Keep approved authoring records, templates and authored static assets in the source repository. Generate the deployable output from those inputs; inspect dependencies before moving legacy files, since older MatterSyn builders treated parts of `dist` as authored inputs. The publication repair migrates these inputs rather than discarding them. Integrate only scientifically and visually reviewed contributions. Unpublished candidates remain outside release inputs.

Use one release snapshot for canonical records, Reader views, training eligibility, the home-page fallback, progress data, and both repository bibliographies. Bind the artifact manifest to the source revision and file hashes; deploy the exact checked artifact. CI must block release when file classes, private-content checks, image provenance/status policy, canonical/public parity, local links or manifest hashes fail. Dynamic Reader anchors need browser checks that the destination exists and scrolls into view after rendering. Do not silence static warnings without checking actual behavior.

Keep source originals and immutable independent audits unchanged when producing a public projection. Record each publication transformation separately. Sanitizing an audit is not a new scientific review; an illustrative reference structure remains distinct from measured specimen coordinates. Progress-only changes and corrections to existing papers are not new completed publications.

Check personal paths in escaped code literals as well as decoded data. Portable command strings must not embed the current machine's executable location. Generated UTF-8 outputs use explicit LF newlines before hashing; byte-copy canonical records, unchanged reviewed metadata, images and CIF/SDF/XYZ inputs. Compare independent Windows and Linux artifacts using exact membership, size, hashes and frozen-input bindings, without normalizing comparison inputs or ignoring files. Report hosted success only after the actual run passes.

Long reader text is not by itself evidence of verbatim quotation. Inspect source similarity before reducing quotations, and retain scientific meaning, sample scope and source locators rather than truncating by character count.

## Git history and delivery

`.gitignore` is a future staging aid; it does not remove tracked files or old history. A historical cleanup is a separate migration: preserve a verified private bundle and working changes first, build and inspect the filtered history in an isolated object store, preserve commit chronology and a private old-to-new mapping, and update only the explicitly authorized remote refs with an expected-head lease. Never reset a dirty scientific checkout or allow an old unfiltered clone to republish it. Routine releases do not rewrite history. Already distributed clones and cached views are outside this local cleanup's control.

Respect the user's existing publication authorization; do not ask again for each reviewed batch. A change in audience or history must be supported by the session's explicit authorization. Keep credentials in the existing credential mechanism, never in source, URLs, manifests or reports. Verify deployment completion and anonymous access, including file hashes, before reporting a release as live.

Changing repository visibility controls current anonymous access to the repository; it does not prove deletion of retained GitHub objects or third-party copies. A Support draft is not a sent request or a guaranteed purge. Follow the user's latest visibility instruction and verify anonymous access after any change.

## Memory and continued curation

Save durable progress and decisions in the public GitHub project at meaningful milestones. Include current source scope, actual extraction/audit/publication status, evidence locations by stable ID, unresolved scientific claims, validated counts, release commits and reusable preferences, without adding complete source documents, raw paper prose, detailed private audits, personal paths, credentials or unrelated app-settings discussion. Public website progress remains a concise reader-facing projection. Keep the full private evidence record separately.

After publication repairs, resume the user's extraction, separate independent audit and CdSe-standard website integration on accepted screening documents. Preserve all variants and sample identities; multiple publications can contribute evidence to one pair without becoming duplicate training examples. Conflicting supported claims remain side by side with citations. Reuse passed unchanged evidence. New arrivals stay separate until their next authorized screening pass. No paid API/cloud run has been authorized. Report achieved throughput and whole-collection denominators rather than treating the 500-paper/day request as demonstrated capacity.

Before freezing a new canonical record and its dependent asset hashes, validate both its schema and the current source-publication projection. Preserve qualitative scientific conditions in the supported qualifier/basis fields rather than a raw-source-text field that the projection removes. Keep numerical values unknown when no number was reported; respect the schema's string/null distinction. Any later representation correction needs a precise delta and refreshed record, Reader, asset and release bindings, without repeating unchanged source audits.

Invoke the actual release CLI `tools/mattersyn-release/gate.py`; `public_release_guard.py` is an importable policy library. A successful process exit without the required hash-bound manifest and report is not a passing release gate. When an invocation fails after unchanged build checks have passed, retain those receipts, verify the exact artifact inventory, and rerun only the failed boundary stage. Never synthesize approval without executing the gate.
